// Public Supabase credentials only — the anon key is safe to expose client-side
// as long as Row Level Security (RLS) policies restrict writes.
// Do NOT put the service key here.

const SUPABASE_URL = "";        // e.g. "https://xxxx.supabase.co"
const SUPABASE_ANON_KEY = "";   // Project Settings → API → anon public key
